name: Gate 9B2C full integration correction

on:
  pull_request:

permissions:
  contents: write

jobs:
  integration:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feat/gate-9b2c-invitation-execution
          token: ${{ github.token }}

      - name: Set up Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v2
        with:
          version: 2.101.0
          github-token: ${{ github.token }}

      - name: Normalize integration projection version type
        shell: python
        run: |
          from pathlib import Path
          path = Path('supabase/functions/command-gateway/tests/integration/invitation-execution.test.ts')
          text = path.read_text(encoding='utf-8')
          old = '          select projection_json, projection_version\n'
          new = '          select projection_json, projection_version::integer as projection_version\n'
          if text.count(old) != 1:
              raise SystemExit(f'projection version query replacement count: {text.count(old)}')
          path.write_text(text.replace(old, new, 1), encoding='utf-8')

      - name: Run full corrected integration suite
        id: integration
        continue-on-error: true
        shell: bash
        run: |
          supabase start >/tmp/supabase-start.log 2>&1
          supabase db reset >/tmp/supabase-reset.log 2>&1
          db_url="$(supabase status -o json | jq -r '.DB_URL')"
          SUPABASE_DB_URL="$db_url" deno test --frozen \
            --config supabase/functions/command-gateway/deno.json \
            --allow-env=SUPABASE_DB_URL \
            --allow-net=127.0.0.1:54322 \
            supabase/functions/command-gateway/tests/integration \
            >/tmp/gate9b2c-full-integration.log 2>&1

      - name: Persist correction or diagnostic
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if [[ "${{ steps.integration.outcome }}" == "success" ]]; then
            rm -f gate9b2c-full-integration-error.txt
            git add supabase/functions/command-gateway/tests/integration/invitation-execution.test.ts
            git add -u -- gate9b2c-full-integration-error.txt || true
            message="test(gate9b2c): cast projection version in integration query"
          else
            tail -n 200 /tmp/gate9b2c-full-integration.log > gate9b2c-full-integration-error.txt
            cat gate9b2c-full-integration-error.txt
            git checkout -- supabase/functions/command-gateway/tests/integration/invitation-execution.test.ts
            git add gate9b2c-full-integration-error.txt
            message="chore(gate9b2c): capture corrected full integration error"
          fi
          if ! git diff --cached --quiet; then
            git commit -m "$message"
            git push origin HEAD:feat/gate-9b2c-invitation-execution
          fi
          if [[ "${{ steps.integration.outcome }}" != "success" ]]; then
            exit 1
          fi

      - name: Stop local Supabase
        if: always()
        run: supabase stop --no-backup >/dev/null 2>&1 || true
