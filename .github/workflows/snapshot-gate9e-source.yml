name: Snapshot Gate 9E source

on:
  pull_request:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build source archive
        run: |
          git ls-files -z | xargs -0 zip -q gate9e-source.zip

      - name: Upload source archive
        uses: actions/upload-artifact@v4
        with:
          name: gate9e-source
          path: gate9e-source.zip
          retention-days: 1
